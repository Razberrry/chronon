export type TimelineRowClasses = {
  wrapper?: string;
  sidebar?: string;
  content?: string;
};

export type TimelineSidebarClasses = {
  sidebar?: string;
};

export type TimelineSubrowClasses = {
  subrow?: string;
};

export type TimelineItemClasses = {
  item?: string;
  content?: string;
  innerContainer?: string;
};

export type TimelineRangeToolbarClasses = {
  toolbar?: string;
  button?: string;
  activeButton?: string;
};

export type TimelineCursorClasses = {
  cursor?: string;
};

export type TimelineAxisClasses = {
  timeAxis?: string;
};
